{
  "//": "Tests run straight from source under `node --experimental-strip-types`, which resolves imports the way the runtime does and so needs the `.ts` extension spelled out. Webpack resolves them itself, so the extension is a type error under the build's config and the tests get their own.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["assets/*.test.ts"],
  "exclude": ["**/node_modules", "_site"]
}
